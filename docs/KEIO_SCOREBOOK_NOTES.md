# 慶応式 / NPB式スコアブック 実験メモ

作成日: 2026-06-10

## 目的

早稲田式の入力データを壊さず、表示だけを慶応式 / NPB式へ切り替えられる実験モードを作る。
開発者が先に試し、8割程度使える状態になったら限定的にユーザーへ開放してFBをもらう。

## 主資料

- NPB公式 `NPB式スコアブックの記入方法`
  - https://npb.jp/scoring/scoring_howto.pdf
- NPB公式スコアリングページ
  - https://npb.jp/scoring/

## 初期方針

- 保存JSONと入力ロジックは早稲田式と共通にする。
- `state.scorebookStyle` を `waseda` / `keio` で切り替える。
- 慶応式はまず画面上のセル表示だけを差し替える。
- PDF出力、集計欄、詳細な公式記録ロジックは早稲田式のままにして、表示実験のFBを優先する。
- UIに通常の設定項目は出さず、隠しコマンドで切り替える。

## 隠し切替

- URLに `?style=keio` または `?scorebook=keio` を付ける。
- 画面上部のバージョン表示を7回タップする。
- 慶応式中はヘッダーに `慶応式 実験中` を表示する。

## 初期実装範囲

- 空セルも慶応式風のセル枠に切り替える。
- 打席結果は慶応式寄りの短い記号に変換する。
  - 三振: `SO`
  - 振り逃げ: `K`
  - 四球: `BB`
  - 死球: `D`
  - 犠打: 送球順や処理結果を四角で囲む
  - 犠飛: `F` + 守備番号を四角で囲む
  - 本塁打: `HR`
  - 併殺: `DP`
  - フライ: `F` + 守備番号
  - ファウルフライ: `FF` + 守備番号
  - ライナー: `L` + 守備番号
  - ゴロアウト: `6-3` など送球順を数字で表示
- 入力結果、走者イベント、一言メモ、保存JSONは既存データを利用する。

## 公式マニュアル照合表

公式資料を見ながら、表示実装の準拠度を段階的に上げるための作業表。

| 項目 | NPB式での扱い | CuViu現状 | 状態 | 次の作業 |
| --- | --- | --- | --- | --- |
| 基本セル | 早稲田式のダイヤモンドではなく、NPB式の枠でプレイを記入。一塁は右上、二塁は左上、三塁は左下、本塁は右下 | 慶応式モードで別SVG表示。塁位置を四隅配置へ変更 | 対応中 | 公式カードに近い寸法・記入位置へ調整 |
| 投球記録 | 球種/カウント欄へ投球内容を記録 | 既存の投球マークを縮小流用 | 仮対応 | NPB式の投球欄位置・記号を再確認 |
| 見逃し/空振り三振 | 三振は `SO`、振り逃げは別記号で扱う | `SO` / `K` に分離 | 対応中 | スリーバント失敗の扱いを再確認 |
| 四球 | `BB` | `BB` | 対応中 | 故意四球を別扱いするか確認 |
| 死球 | `D` | `D` | 対応中 | 既存JSONの `DB` との対応維持 |
| フライアウト | `F` + 守備番号 | `F8` 形式へ修正 | 対応中 | 守備番号の位置・補助表記を確認 |
| ファウルフライ | `FF` + 守備番号 | `FF5` 形式へ修正 | 対応中 | ファウル種別の入力導線を確認 |
| ライナーアウト | `L` + 守備番号 | `L6` 形式へ修正 | 対応中 | ダブルプレー絡みの表示を確認 |
| ゴロアウト | `6-3` など送球順 | `4G` を廃止し送球順へ修正 | 対応中 | 単独処理時の表記を確認 |
| 犠打/犠飛 | アウト種類を四角で囲む | 慶応式では結果ラベルを四角囲み | 仮対応 | 囲む対象を公式例に合わせる |
| 併殺 | `DP`、走者アウト経路とあわせて記録 | `DP` を別注記し、走者イベントを右欄に仮表示 | 仮対応 | 走者側のアウト記録をNPB式へ整理 |
| 失策 | `E` と責任野手、進塁関係を記録 | 早稲田式の `E` を流用 | 未対応 | 失策と自責/非自責を分ける保存項目を検討 |
| 走者進塁/得点 | 打者結果とは別に走者経過を記録 | `二進`、`三進`、`得点` を該当塁の近くへ仮表示 | 仮対応 | 公式カードの走者記入位置へ移す |
| 走者アウト | 走者欄にアウト経路を記録 | `走者アウト 6-5` などをセル下部へ仮表示 | 仮対応 | 塁ごとの位置に割り振るため、走者アウト地点を保存項目に追加する |
| 残塁 | イニング終了時の残塁を記録 | `残塁` をセル左下へ仮表示 | 仮対応 | 公式カードの残塁欄位置を確認 |
| 盗塁/盗塁死 | 走者欄に記録 | 早稲田式イベントから右側小欄へ仮表示 | 未対応 | NPB式の走者欄位置へ移す |
| 代打/代走 | 選手交代と打席のつながりを記録 | 早稲田式ロスター欄中心 | 未対応 | PH/PRのセル表示を追加 |
| 投手交代 | 投手欄と交代時点を記録 | 慶応式では青波線をやめ、セル上端に `P:氏名` を仮表示 | 仮対応 | 公式カードの交代時点記入方法へ寄せる |
| PDF出力 | NPB式カードとして出力 | 早稲田式PDFのまま | 未対応 | 実験モードが安定後に着手 |

## 自動運転時の優先順

1. 公式資料と明らかに違う早稲田式の残骸を消す。
2. 打席結果の記号をNPB式に近づける。
3. 走者アウト・進塁・得点の記入位置を整理する。
4. 投手交代・代打・代走など、試合進行の記録へ広げる。
5. 最後にPDF出力へ反映する。

## 未実装・要検証

- NPB式の細かな投球欄・走者欄・失策欄の正式配置。
- NPB式PDF出力。
- 自責点、投手責任、公式記録向けの詳細集計。
- 代打・代走・守備変更のNPB式表記。
- 早稲田式と慶応式の同一打席データからの完全な相互表示。

## 変更履歴

- v0.1.107: 慶応式セル右下に出していた開発用の `NPB` 表示を削除。走者経過の仮表示を `R/A2/O/LOB` から日本語表記へ変更。
- v0.1.108: 慶応式セルの塁配置を一塁=右上、二塁=左上、三塁=左下、本塁=右下へ変更。安打・四球・死球・本塁打などは到達塁の位置へ結果を表示。走者の進塁・得点メモも該当塁の近くへ移動。

## ユーザー開放の目安

- 主要結果（安打、二塁打、本塁打、三振、四球、犠打、犠飛、併殺）が慶応式表示で大きく破綻しない。
- 早稲田式に戻してもデータが変わらない。
- 保存・復元・試合データ書き出しで表示モードが保持される。
- 実験中であることが画面上で明確に分かる。
